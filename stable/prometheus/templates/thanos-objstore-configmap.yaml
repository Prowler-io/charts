{{- if and (.Values.thanos.enabled) (.Values.thanos.objStore.bucketName) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: gcs-bucket
data:
  gcs-config.yaml: |
    type: GCS
    config:
      bucket: {{ .Values.thanos.objStore.bucketName }}
{{- end }}
